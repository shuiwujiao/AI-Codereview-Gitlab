services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: app
    image: shuiwujiao/ai-codereview-gitlab:1.0.0
    ports:
      - "5001:5001"
      - "5002:5002"
    volumes:
      - ./data:/app/data
      - ./log:/app/log
      - ./conf/prompt_templates.yml:/app/conf/prompt_templates.yml
    env_file:
      - ./conf/.env
    restart: unless-stopped